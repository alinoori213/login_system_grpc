syntax = "proto3";
package authentication;

service Authentication {
  rpc Authentication(Auth) returns (UserPk);
}

message Auth{
  string username = 1;
  string password = 2;
}

message UserPk {
  int64 pk = 1;
}